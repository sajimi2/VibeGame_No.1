# 当前状态 · 2026-09-20

本文件只保留当前有效状态，历史阶段记录见 Git。入口 `scenes/battlefield.tscn`，固定斜角正交、十二朝向、移动攻击、五敌人与取信交付闭环；保留 `scenes/tactical_height.tscn` 试验场。

## 最新实验：简单 Blender 小屋 + 生成美术（已接入可玩场景）

- 用户要求简化前墙并继续实验。本轮将门左/门右/门楣/三角山墙归到同一个 FrontWall 实体，保留原门洞几何与碰撞，透视不再逐小板分别开关。已接入生成的正面、侧面、屋面三张图片；不是继续细雕网格，也不是用整张概念图替代场景。入口 `tools/cottage_art_lab.tscn`，F6；V 灰模/美术，1/2 跳到两栋门前，F2 环境对比，F11 全屏。两栋同一预制件独立透视。正式地图未换，不启用敌人、任务或进度。
- 简模源 `assets/environment/experiments/cottage/source/cottage.blend`，保存 5×6m 墙体、1.5×2.2m 门洞、两片独立坡屋面、地板和箱子；源目录 .gdignore 防止隐式 Blender 导入。`tools/blender/cottage_experiment.py` 默认只从已保存源导出 GLB；--init 在源存在时拒绝覆盖。GLB 网格由实验预制件装配为显示与碰撞，Support 地板排除透视。外地面降低 2cm 消除共面闪烁。
- `art_proposal_v1.png` 仍保留为风格提案；内置 imagegen 另生成 `textures/{front,side,roof}.png` 作为实际材质，完整提示词在 `texture_prompts_v2.txt`。`cottage.gd` 按资产局部位置生成 UV/外侧掩码，不随复制位置漂移；内部复用灰泥/木地板/箱子材质。共用 `art_surface.gdshaderinc` 仅换表面颜色，旧材质默认关闭；屋顶新增可选表面材质入口，沿用现有深度/阴影/透视；wall_occlusion 同步 V 对比参数到显示副本。图片细节不能改变简模轮廓，复杂雕饰仍需独立部件；映射目前按该屋尺寸编排。
- 本轮真实 GPU 专项 `cottage_art_lab` 29 项通过，覆盖原行走/碰撞/阻弹/透视、实际贴图保留、整面前墙统一响应、V 对比、两栋 UV 一致和状态隔离；已查看外观、灰模、室内、背墙、近前墙、第二栋截图。`work/cottage/art_exterior.png` 是实装截图，提案不作为运行证据。回归 environment_art 20、selective_wall 10、occlusion_presentation 24、environment_resolution 22 项通过，错误日志为空；日志 `work/cottage_art_final.log` / `work/cottage_regression_*.log`。本轮导入、启动 8 项及差异检查通过。未提交/推送，最终风格和移动观感由用户试玩。

## 前一轮：仓库旁非遮挡矮墙被连带淡化

- 用户指出仓库西侧走道的石矮墙没有挡在人物前却消失。实际复现确认：原材质副本按材质资源共用，全局墙圆开启后，圆内其他墙也收到淡化信号；近墙深度柔边无法区分独立构件。上一轮用途保护解决了地板误删，但没有解决非遮挡墙段的筛选。
- `wall_occlusion` 现在收集相机到当前身体采样点之间命中的全部构件：覆盖并集负责 90%/80% 开关，命中集合负责逐构件淡化。墙身/压顶/窗框按最近物理父节点共组，无物理节点则归美术构件或独立网格；材质按构件隔离，Shader 仍共享。保留原半径、中心全透、22% 残影、深度柔边和承托保护，旧遮挡退出有缓冲；未激活材质跳过圆心等参数更新。物理与玩法未改。
- 新 GPU 专项先复现同材质旁墙/近后墙分别被误改 626/628 像素，修正后均为 0；最终 10 项通过，另覆盖重叠两堵前墙、圆仍开启时旧墙独立恢复，以及真实仓库南北走道的矮墙/压顶。已查看三处真实仓库截图，左侧石矮墙完整保留。证据 `work/selective_before.log`、`work/qualified_selective_wall.log`、`work/occlusion/selective_warehouse_{before,after}_*.png`。
- 本轮实际 GPU 回归墙圆 21、近墙折角 3、承托面 15、灰色/屋顶 24 项全部通过；导入、启动和差异检查通过，专项错误日志为空。未提交/推送，最终移动观感留待用户试玩。

## 前一轮：透视误删高台与漏掉薄墙帽

- 用户报告透视漏面、露出地下草地、人物似乎悬空。根因有两处：普通遮挡注册只按材质与厚度过滤，石砌高台和墙共用 `masonry` 而误入透视；低于 0.25m 的网格被一概跳过，矮墙压顶/薄横梁留下孤立面。上一轮改深度柔边并未解决构件用途误分类，之前专项也漏查了承托结构。
- 新增场景元数据 `occlusion_role="support"`，对整组承托结构向下继承。驿站地面、高台、南北坡桥及三栋地板显式标记；程序生成高台/坡道和木桥构件同步带标记。透视注册先检查用途，保护顶面/侧壁/支架，不改物理或尺寸。取消 0.25m 厚度排除，薄墙帽/横梁与墙身一致参与；共享材质资源不变。
- 新 `occlusion_support` 实际 GPU 专项先在旧版复现 15 项中 9 项失败（石台、两座木桥被透视影响，薄压顶残留 360 像素），修复后 15 项全部通过。开关透视时真实石台/地板/双桥画面逐像素不变，压顶核心残留为 0；已查看四处仓库边缘的前后截图。日志 `work/support_before.log` / `support_after.log`，画面 `work/occlusion/support_{before,after}_*.png`。原两圆尺寸、中心全透、22% 残影、灰色提示和开启阈值保持。本轮未提交/推送。
- 本轮导入与差异检查通过；实际 GPU 回归墙圆 21、近墙折角 3、灰色/屋顶 24、环境 20、人物精度 22、真实驿站行走 24 项全部通过，日志 `work/support_<专项>.log`，运行错误日志为空。没有可调用的 Godot MCP，以上来自本机独立引擎，不代替用户最终观感验收。

## 前一轮：透视圆中心全透、近墙折角与尺寸

- 按用户要求两类圆半径统一放大 50%：2.25m → 3.375m，外缘柔边同步从 0.85m → 1.275m。墙圆中心半径约 0.91m 完全透出，随后平滑增加至 22% 墙面覆盖，再在外缘恢复正常墙体；室内整顶 22% 淡化、90% 开启/80% 退出阈值不变。
- 折角根因是屋顶和墙体 Shader 均用经过人物中心的硬深度平面切断透视。贴近厚墙/斜面时，这个平面穿过可见墙面，在圆内留下直线和折角。共用 `occlusion_depth.gdshaderinc` 改为 0.85m 身体/墙厚余量加 1m 柔和深度过渡，远后方背景仍不淡化。不改几何、碰撞或敌人判定；灰色仍按实际剩余遮挡显示。
- 之前验证仅覆盖远离中心深度平面的前景墙，漏了贴墙边界；新增真实 GPU 斜墙/斜屋面复现。旧斜墙中心 3390 个受检像素中残留 2081 个，新版为 0；另验证远后方墙保持不变。墙圆专项 21 项通过，实际检查全透中心、约 11% 过渡中段/22% 残影环、扩大半径、移动和全屏；回归灰色/屋顶 24、环境 20、人物精度 22 项通过。截图 `work/occlusion/near_*`，日志 `work/near_wall_after.log` 与 `work/gradient_*.log`。
- 专项释放临时墙时另发现采样先向强类型变量赋值、后检查失效实例的顺序问题，现先校验再访问，动态构件移除不再报错。本轮未提交/推送，观感待用户试玩。

## 上一轮接入：大面积墙体遮挡的第二层透视圆

- 用户确认上一轮逻辑通过，并要求仓库屋外被背墙全挡时也能观察地形。现保留顶层屋顶全透圆，额外增加同中心/同半径的普通墙体淡化圆：身体轮廓的普通遮挡采样比例达到 90% 开启，低于 80% 关闭，约 0.28s 渐变、0.16s 退出缓冲。圆心保留 22% 墙面覆盖，与室内屋顶一致；小范围遮挡继续逐像素灰色提示。
- `level` 只装配 `wall_occlusion`；模块每秒 10 次从玩家当前帧颜色/深度图集采样（每 3 个源像素一个网格点，上下身重叠只计最前表面），与注册环境的原始三角面求交并合计多个遮挡物。比例是离散面积估计，不是整屏精确像素计数。屋顶不计入；淡化后的深度不参与触发，避免自身开关振荡。只缓存静态玩家图页，不读回屏幕、不新增渲染视口。
- 当前接管 `environment_surface` 和 `pixel_stone` 实心建筑/墙柱/巨石网格，跳过草地/土路/薄地板和屋顶。显示材质局部裁切，原材质阴影代理保持；碰撞、导航、敌人视线与命中不变。Sprite3D 树/其他透明材质仍沿用灰色提示，不把透明图案按整块矩形计入。动态构件通过 `register_branch` 显式注册。
- `occlusion_style` 保存两种圆共享的默认半径、柔边、覆盖率和渐变时间；有屋顶遮挡时墙圆同步该屋顶实际 Inspector 设置，无屋顶时用共享默认值。退出/重开不写存档。
- 本机实际 GPU 新专项 19 项通过：半身不触发、两个遮挡合计触发、无碰撞网格、持续淡化不振荡、12 朝向站/跑/蹲/跳、原始/F2/全屏、仓库背墙与室内叠层、恢复和运动稳定性。圆心实测残留约 22.1%，圆外与阴影零差异；指定 9 步平移序列对齐后墙面显著跳变为 0。48 种姿态二次采样最坏约 0.68ms（不含首次静态图页载入，不是全游戏帧耗时）。回归局部灰色/屋顶 24、环境 20、人物精度 22、驿站通行 24 项通过。证据 `work/wall_*.log`、`work/occlusion/warehouse_two_circles.png`；本轮未提交/推送，具体观感待用户试玩。

## 已通过逻辑试玩：局部灰色与两层屋顶透视

- 用户用四张参考图确定规则，取代此前“按建筑固定揭顶”的建议：小遮挡仅把被挡住的角色像素显示为灰色；室内整组屋顶先保留低不透明度，再叠透视圆；室外只使用透视圆。本轮按这三条规则实现，不推断参考游戏内部算法。
- `baked_human` 的 `layers.occlusion` 替代旧 `layers.outline`。同一 Shader 用 `OCCLUSION_PASS` 变体，按烘焙表面深度与实际环境深度比较，仅绘灰色被挡部分；先绘灰色且不写深度，再绘正常身体，所以露出的头手不染灰、上下身不叠出亮缝。中心物理射线的 `occluded` 仅保留 HUD/调试语义；显示改由 `refresh_occlusion_visibility()` 与 GPU 裁切负责，脚/手被挡但中心射线漏检也有效。玩家及六类敌人共用，死亡关闭提示。角色图集与玩法未改。
- `interior_roof` 新增 `indoor_opacity=0.22` 与独立 `interior_fade`。可进入建筑的 `interior` 范围决定整组屋面淡化，真实视线仍独立控制圆孔；两层覆盖率相乘，不隐藏整个屋顶节点。约 0.28s 渐变、0.13s 离开缓冲。低不透明度通过世界投影锚定的像素覆盖率裁切实现，保留原深度、粗环境合成顺序与完整阴影；没有逐帧 CPU 图片处理。不可进入塔顶/屋檐外仍仅开圆，不自动改墙体。
- 实际 GPU 遮挡专项 24 项通过：窗口/全屏半身遮挡、可见头手与轮廓外零误染、中心射线漏检、F2 两档灰色像素一致、室内外/屋上/退出/独立建筑、屋面残影与额外圆孔实际出图、开孔后不染灰。半透明屋面平移对齐后最坏显著跳变约 0.031%。回归环境 20、人物精度 22、角色 71、骷髅 24、武器 70、塔楼反馈 5、全屏 15 项通过。截图 `work/occlusion`，日志 `work/environment_layers/*_layered.log` 与 `occlusion_final.log`；目前无可调用 Godot MCP，使用本机独立引擎验证。主观灰色强度/屋顶淡化效果待用户试玩，未提交/推送。

## 最新修正：仓库端柱共面与遮挡方案调研

- 仓库南侧墙端木柱与灰泥墙芯的端面完全重合，实际 GPU 复现材质随镜头移动跳变；不是上一轮环境采样错误复发。`architecture_piece.wall` 中端柱向外包边 0.03m，柱上下各超出墙芯 0.02m，消除异材质共面。复用构件统一修正，门洞、碰撞和导航未改。
- 相同 12 步平移中，仓库柱面 ROI 明显跳变平均 2.124%、最坏 3.676%，修正后均为 0。修正前后实景在 `work/warehouse_post/{before,after}`；移动专项补柱面小区域断言，避免被全画面均值掩盖。原始/粗环境两档柱面均零跳变，环境移动专项 26、屋顶/环境 20、驿站真实步行与门洞 24 项通过，导入无错误。本轮不改遮挡呈现策略，不提交/推送。
- 当时确认冲突来自中心射线开启全身描边与屋顶独立开孔。此前建议固定建筑组揭顶/切上半墙，现已被用户确认的上方“低不透明度屋顶叠加透视圆”方案取代，不继续沿用旧建议。

## 最新修正：粗环境移动时的闪烁

- 用户反馈砖缝、瓦缝及草地/巨石在移动时闪烁。复现确认：旧合成在每个 2×2 块的整数坐标中心做归一化最近邻读取，恰好落在源纹素边界；浮点舍入使相邻像素被不稳定地选中。只抽一个点也会漏掉细缝。之前的世界网格锚定没有解决源纹素边界，静态精度检查又未覆盖时间稳定性，这是此前验证遗漏。
- `environment_pixel_pass.gdshader` 改为整数 `texelFetch` 精确读取同块四个源像素并做面积平均。保留 2×2 硬边色块、世界锚定、原环境深度和 F2 原始/粗环境开关；不改相机、角色/武器图集、世界材质、物理、战斗或存档。只在块内部平均，不做跨块插值或历史帧混合；细纹理会略柔和，不引入时间拖影。
- 本机 GPU 同一 12 步斜向平移复现中，抵消正常画面位移后，RGB 任一通道差异超过 8/255 的像素占比，旧版平均 4.756%，精确单点 0.016%，块内平均 0.009%；证据 `work/environment_motion/{old,center,area}`。这是指定序列的测量，不代表任何地图绝对零闪烁，也不是 FPS 提升。
- 新 `environment_motion` 专项 24 项通过：窗口/全屏、墙瓦与野地两处、横纵/斜向、F2 两档；固定角色动画/屋顶渐变，比较同一世界表面的真实 GPU 帧。粗环境最坏帧跳变比例约 0.014%，指标 `work/environment_motion/regression/metrics.json`。人物精度专项 22、屋顶/环境专项 20 项本轮重跑通过；七角色身体/武器仍逐像素一致。源码修正只涉及环境合成 Shader；新增动态回归，具体移动观感待用户重开游戏试玩。未提交/推送。

## 最新调整：只降低环境显示精度

- 用户要求保留当前人物精度，仅粗化环境并保留对比开关。公共关卡装配加入 `presentation/environment_pixel_pass`：默认将环境颜色按 2×2 逻辑像素合并采样（现采用上方面积平均修正），等效 640×360；**F2** 切换粗环境/原始环境，提示显示三秒，暂停仍可用，重新进入关卡默认开启。不改 96² 角色图集、体型、镜头、战斗或存档。
- 草地、建筑、石材、树与世界阴影先采样；身体、武器、刀光和箭矢随后按原精度显示，保持真实深度测试。人物/武器纸片始终写入各自深度，描边单独不写深度；箭矢保留独立裁剪阴影和透明淡出。HUD、血条与文字保持原精度。像素网格锚定世界相机平面，减少相机平移时地表采样跳闪。
- 本版是选择性的环境颜色后处理，不是降低整个三维视口分辨率；原环境深度保持原精度，避免角色穿墙。没有新环境 SubViewport、场景副本或逐帧 CPU 图片读回，也不宣称 GPU 性能提升。透明尘土、接触阴影等仍在后续特效阶段绘制；新增透明环境材质需明确加入哪一阶段。
- 实际 Compatibility GPU：新环境精度专项 22 项通过，七角色在窗口和全屏下，开关前后的可见身体/武器像素与轮廓逐像素相同；环境实际形成 2×2 同色块。回归角色 71、武器/刀光 70、插箭 40、图集 30、屋顶/环境 20、窗口全屏 15 项通过。日志 `work/environment_layers`，七角色对照图 `work/environment_resolution`。没有可调用 Godot MCP，证据来自本机独立引擎；视觉颗粒与移动舒适度待用户试玩。本轮未提交或推送。

## 最新调整：全屏基准与显示切换

- 用户指出全屏四周黑框：原 1280×720 整数缩放将 1080p 所需 1.5× 取整成 1×，此前测试只确认窗口占满屏幕，漏了内容面积。现改保持宽高比的小数倍率铺满，未改逻辑画布、相机、图集或玩法。实际 GPU 专项新增内容面积断言，共 15 项通过；本机 1920×1080、内容变换 1.5×，鼠标投影、屋顶圆心、暂停/重开与窗口恢复通过。日志 `work/display/fill.log`。运行中的旧进程需退出后重新启动。

- 用户反馈 F11 / Alt＋Enter 无反应：实际正在编辑器以 `--wid` 内嵌运行，日志证实引擎拒绝全屏。此前 `Window.is_embedded()` 未识别这种原生嵌入，独立进程验证漏了用户入口。改用 `Engine.is_embedded_in_editor()` 检测，内嵌按键显示非模态操作提示；不强拆编辑器宿主，不修改全局编辑器配置。真实原生父子窗口内嵌专项 4 项、独立切换专项 14 项重新通过，日志 `work/display/embedded_child.log` 与 `standalone.log`。全屏须用独立游戏窗口；旧运行进程需重开才能加载提示改动。

- 用户提出以全屏为主要美术验收基准，同时兼顾窗口。现按上方最新要求仅将环境显示等效到 640×360，保留现有 96² 图集、Blender 制作源与镜头倍率；没有实施整体画面或 320×180 降采样。
- F11 / Alt＋Enter 切换原生游戏全屏，返回恢复窗口大小、位置或最大化状态；暂停时可用，R 重开保留恢复记录。`ui/window_mode` 只挂在 Window 下处理显示，关卡只负责安装。未新增存档设置或自动全屏启动。
- 修正此前仅看 project.godot 的判断：`level._ready` 实际覆盖成 1280×720、viewport、整数放大。最初补固定宽高比但保留整数放大，在 1920×1080 全屏只显示 1× 并留黑边；现已按上方记录改为 1.5× 铺满。以后若采用 640×360 可在该屏幕整数放大 3×。
- 测试发现窗口大小变了但逻辑画布没变，静止鼠标不会可靠触发画布 size_changed。玩家现每物理帧读取转换后的鼠标坐标，保留测试模式的显式输入，避免切换后首次瞄准沿用旧值。
- 实际 GPU 显示专项 14 项通过：窗口/全屏、完整显示器尺寸、暂停、最大化、场景重开、窗口恢复、静止鼠标瞄准与屋顶圆心对齐；F5 启动 8、空间战斗/实际输入 27 项通过。日志 `work/display`；非编辑器内嵌实测。当前全部改动仍未新增提交。

## 环境与建筑像素美术、屋顶局部透视

- 用户确认全部新敌人接入成功。先将已验收玩法提交为 `2632f64` 并推送 `origin/codex/skeleton-blender-96`，作为本轮美术前的回退点；本轮环境改动尚未提交，等待视觉试玩。
- 驿站改为灰泥木骨建筑、石墙/箭窗/旗帜、瓦顶、木箱和推车；草地加入低对比草簇，土路取消荧光色带与地面网格线。南北几何坡改木架坡桥，荒堡和高度试验场也共用新草地、地形材质及坡桥。认可的人物、树和巨石继续使用。
- 新 `assets/environment` 集中 11 张 128² PNG、共用材质与六种可调尺寸构件；`environment_library` 负责资源，`architecture_piece` 只生成按材质合并的视觉网格。构件预览 `tools/environment_preview.tscn` 可 F6；纹理离线工具只在显式调用时覆盖资产，游戏不绘制纹理或新增环境烘焙视口。维护见 `docs/ENVIRONMENT_ART.md`。
- 屋檐 bug 根因：地面室内 AABB 漏过投影覆盖，屋面又没有物理碰撞供人物描边检测。改成相机对玩家脚/胸/头与屋面盒形网格的视线检测，人物周围打开柔边像素透视圆；半径 2.25m、过渡宽 0.85m、渐变 0.28s、退出缓冲 0.13s。圆外屋面及独立阴影代理不变，前墙继续走原描边。
- 原桥坡连续碰撞、门洞宽度、敌人/武器/存档保持。桥下仍为实心坡碰撞，不开放多层通行，扶栏仅显示；未来非盒形屋顶需同步修改遮挡形状，不应直接用整模型 AABB。
- 当前验证：导入通过；环境专项无头 18 / 实际 GPU 20 项、驿站路线 24、混合遭遇无头 / 实际 GPU 各 27、坡缘 56 条、空间战斗 27 均通过。环境 GPU 专项实际比较开孔前后像素，确认圆内改变、圆外和太阳阴影不变；已查看驿站、荒堡、试验场及构件预览画面。日志与截图在 `work/environment`。旧空间战斗测试退出仍有 ObjectDB 清理警告，无脚本错误。
- 本轮无可调用 Godot MCP；以上为独立 Godot 进程与真实 GPU 验证，进度隔离。美术质量、遮挡舒适度与交战时的观感仍等待用户试玩。

## 已通过试玩：三种新敌人

- 用户认可静态像素方向后，授权一次接入哥布林、石头人、史莱姆的动作和战斗。三份正式源保存于 `assets/characters/blender/*_rig.blend`；旧候选初稿保留作设计参考。现有玩家等四份制作源未改写。
- 每种完成待机、移动、攻击、受击、死亡五组动作，96×96、十二朝向，各 1,188 帧；显示比例沿用确认基准。哥布林 16 骨（含矛骨）、石头人 15 关节刚性石块、史莱姆三骨压缩回弹。三者用整身颜色/深度图集，复用播放/阴影/遮挡/补色路径；运行时没有新增角色烘焙视口。
- 哥布林生命 55，0.65 秒锁向蓄势、矛刺伤害 23，收招后短暂后撤；石头人生命 140，1.1 秒蓄力并显示落点，砸地半径 1.6m、伤害 39，轻击不打断蓄力、重刀强击退可打断，离圈/跳跃/墙体可避伤；史莱姆生命 36，0.7 秒压缩预告后直线冲撞，真实接触伤害 16，每次只命中一次，撞墙或前方无地面时停止。伤害含驿站 1.3 倍倍率，原战场难度不变。
- 驿站总数仍十名：西侧野地一哥布林一史莱姆，外院一哥布林一守卫一弓手，仓库一守卫一弓手，内院一石头人一史莱姆一弓手。F6 / `play_waystation.bat` 试玩；F5 仍为旧战场。三种近战武器、营地恢复、R 重开、室内屋顶与隔离进度沿用。
- `creature_profile` 保存各物种体型/时序/范围；`creature` 复用感知、导航和受击，推进三种独立攻击。玩家弓箭辅瞄按实际身体高度取点，避免瞄向史莱姆上方。`art_preview` 新增三种来源，支持整身原尺寸导出/补色回导及实际 3D 源动作预览；不伪造上下身或握点。
- 七份 Blender 源独立重开验证通过；实际 GPU 烘焙无空帧/裁边，已查看动作表和游戏命中/外院画面。新敌人无头专项 42、驿站混合遭遇 27、图集回导 30、角色覆盖 66、旧 AOE 23、守卫反应 28、空间战斗/辅瞄 27、F5 启动 8 均通过。实际渲染专项：新敌人 42、驿站 27、角色播放/遮挡 71、插箭 40、AOE 23 均通过。部分已有测试退出仍有 ObjectDB 清理警告，无脚本错误；主观手感随后已由用户确认通过。日志及图在 `work/creatures`。
- 本轮修正制作时父骨矩阵更新顺序，避免子骨错位；抬臂动作调整后全部朝向通过边框检查，并补上落拳屈膝接地。追坡测试最初被留在坡顶的测试守卫挡视线，隔离测试演员后，三种真实体型均通过门洞和上坡，未修改导航绕过问题。
- 用户确认三种新敌人接入成功，要求先提交推送作为回退点，再推进环境/建筑美术与屋顶局部透视。当前无可调用 Godot/Blender MCP，既有验证来自独立引擎与 Blender 进程。

## 已通过试玩：近战 AOE

- 用户要求完善群攻后自行试玩。宝剑横斩改为约 100°、最多三人，突刺与匕首维持单目标；重刀维持原下砸动作，正面窄劈后在真实落点 1.25m 内产生冲击，整次攻击合计最多四人。刀刃原伤害不变，冲击为基础伤害的 75%（当前 20），击退初速 5m/s；盾挡仍减伤及削弱击退。玩家在原战场和驿站均可使用这些能力，敌人攻击未改成群攻。
- `action_profile` 保存上限/击退/冲击范围；`melee_query` 负责近战多目标与环境阻挡，原箭矢首个命中规则保持。`player_combat` 共享整次动作去重与名额，先被刀刃打中者不叠加落地伤害，挥刀按动作进度逐侧结算。冲击要求实际接地、脚底高度差不超过 0.55m，并同时检查玩家到目标、落点到目标的遮挡；敌人胶囊不再误当落地表面。
- 无头与实际 GPU 渲染 AOE 专项各 23 项通过；相关回归为 F5 启动 8、动作时序 15、空间战斗 27、守卫反应 28、武器编排 45、驿站强化遭遇 21，均通过。已查看群攻画面，导入与差异空白检查通过；用户随后确认本轮测试通过。测试使用隔离场景与进度；当前没有可调用 Godot MCP。
- 以上玩法已纳入回退提交 `2632f64`；后续仍可根据包围战中的横扫覆盖、重刀解围力度与盾挡表现调整数值。

## 前一阶段：驿站强化遭遇已通过试玩

- 用户已通过驿站布局、西侧野地和室内渐隐屋顶评审，本轮要求只接入已有敌人并提高难度。独立 `scenes/waystation_blockout.tscn` 默认进入战斗，F6 或 `play_waystation.bat` 启动；F5 原战场保持原难度和任务。
- 78×78m 场地、1.8m 高台、双向缓坡及三栋可进入建筑保持；上一轮为六名守卫、四名弓手，本轮组成已替换为上方混合驻军。
- `data/encounters/waystation_hard.tres` 集中调参：守卫生命 80、弓手 52；伤害 1.3 倍（横斩 26、突刺 33、箭 20），前摇 0.85 倍、收招 0.78 倍、起手冷却 0.8 倍、追击 3.3m/s。横斩前摇仍有 0.272 秒，搭箭、挥出命中窗口、盾挡和打断保持。18m 离岗半径或玩家回营地触发归队，减少跨区串怪。
- I 背包提供既有匕首、宝剑、大砍刀；返回出生点 2.1m 内恢复生命，R 重开。HUD 显示剩余人数及命中反馈；击败十人给出完成提示。无任务/奖励，不读写真实成长或存档。战斗模式禁用 M/1–5 观察冻结及瞬移；根节点关闭 `Combat Enabled` 可恢复空间勘察。
- 敌人专项无头与实际 GPU 渲染各 21 项通过，覆盖真实箭/近战扣血、门洞追击、高台追击、实际归队、营地恢复与隔离装备；已查看外院实景。测试连续瞬移曾令物理碰撞不同步，已在测试装配时等待同步，地图导航未作绕过修补。当前没有可调用 Godot MCP，证据来自独立引擎；难度和手感等待用户试玩，不将自动通过当作体验验收。
- 相关无头回归通过：F5 启动/默认难度 8、守卫遭遇 28、盾挡反应 28、动作时序 15、战斗修整 15、插箭 34、灰盒路线/屋顶 31；导入及差异空白检查通过。音频输出测试需真实时间推进，初次固定帧加速导致其失败，正常计时复跑通过。
- 此阶段原屋顶采用室内 AABB 控制整顶渐隐；当前已被上方的视线检测与局部透视取代。场景静态几何仍可在编辑器调整。
- 此阶段布局与遭遇已纳入回退提交 `2632f64`。

## 已确认的制作标准

- 用户已认可 Blender 骷髅骨架复用与 96×96 效果，并批准玩家、守卫、弓箭手的分辨率比较。本轮三者现有全动作已重烘焙并接入 F5/F6：玩家 19,332、守卫 9,324、弓箭手 7,956 个分层帧，原骷髅 9,324 帧继续使用。
- 正式路线：**Blender 建模/骨骼/动作 → GLB → Godot 开发期烘焙 → 96² 颜色/深度/握点图集**。世界采样范围 2.56m、十二朝向，屏幕观察大小保持用户确认的 `VIEW_ZOOM=1.2`；精度提高不改变身体、碰撞体或地图尺寸。
- 原四份可编辑源与本轮三份新物种均集中在 `assets/characters/blender/`，日常工具为 `tools/blender/export_characters.py`、`validate_characters.py` 和 `tools/bake_characters.gd`。导出工具只读保存的 `.blend`；校验检查实际源骨架、权重、动作和表面运动。
- 共用 15 骨名称及采样协议；各文件保存独立动作。仍以刚性单骨权重分段为主，不宣称任意比例自动重定向或完成软蒙皮。后续非人形/Boss 按需要另选骨架。
- 宝剑实体统一缩小 20%，玩家/守卫共用；封闭厚度、装饰、阴影及刀光端点同步缩小。伤害、攻击距离和时序不变。

## 架构与清理

- 制作层负责源模型和姿态采样；工作台负责查看/导出/回导；`baked_human` 负责游戏身体播放、深度、阴影及世界握点。角色运行时不实例化源骨架或角色烘焙视口；武器/刀光仍使用各自实时 GPU 像素化。
- 移除旧直接二维人物绘制、姿态/纸片适配器、32×48 来源、旧 Godot 程序建模种子、一次性迁移/分辨率比较工具、旧实验入口及说明。旧二维握点换算和运行时回退已退出；动作资源移除不再消费的二维身体曲线，保留战斗时序和武器旋转。
- 像素树提取为独立 `tree_art.gd`；当前箭矢、武器、石材、碰撞、库存、存档和有效回归保留。三种候选静态初稿集中在 `assets/characters/candidates/`，正式源已转入 Blender 制作目录。
- 工作台支持七角色的像素/3D 源预览、原尺寸导出与补色；原类人保留分层和握点校准。旧 32×48/64² 手工稿不套用到 96²；人工导出目录 `work/art_atlases` 保留。补色不能替代改形体后重新烘焙。
- 清理前非忽略资产快照在 `work/art_finalize/before_cleanup.zip`。旧制作目录、旧候选/工具及 719 项历史临时文件移到工程外 `D:\vibe coding\OutpostRPG_archives\20260919_art_finalize`，移出清单在 `work/art_finalize`。不清理用户存档、个人配置、插件或另一个学习工程。

## 已完成美术定版验证（历史记录）

- Godot 4.7.2 Compatibility 导入通过；当前完整无头专项累计 847 项断言/轨迹检查通过（含 290 条贴石/绕石检查和 56 条坡缘轨迹）。最初失败的图集数字类型比较、动作首尾重复姿态、缓冲 Ctrl 输入检查已修正并独立复跑。
- 实际 GPU 渲染：启动 7、角色 59、骷髅 24、像素武器 70、动作编排 47、插箭 40、图集回导 24，全部通过。检查了人物/武器实景、动作序列和图集；缩剑后的墙挡测试根据当前刀尖确定中段，不再固定使用旧刀长位置。
- 四份 Blender 源独立重开，骨架、动作、权重、材质和真实手部变形验证通过；资产引用扫描无缺失，图页数量与清单一致。
- 少数已有测试退出仍有 ObjectDB/资源清理警告，无脚本错误。本轮工具列表没有可调用的 Godot/Blender MCP，以上来自本工程独立引擎/Blender 进程，不是编辑器 MCP 验收。测试进度隔离，未改真实存档。
- 用户已批准此前模型/尺寸与主要玩法；本轮完成后的完整手感仍以用户试玩为准。未新增下一阶段玩法、技能或敌人行为。

## 接下来从哪里看

制作/回导：[docs/ART_PIPELINE.md](docs/ART_PIPELINE.md)；代码职责与隐藏接口：[docs/ARCHITECTURE.md](docs/ARCHITECTURE.md)；方向：[docs/TACTICAL_HEIGHT_PLAN.md](docs/TACTICAL_HEIGHT_PLAN.md)。

角色美术定版 `8eba0f8`、已验收驿站玩法 `2632f64` 均已推送 `codex/skeleton-blender-96`。本轮环境美术留在工作区供试玩，先确认建筑/草地/桥的统一性和屋檐透视，再推进关卡目标。以后新增重要逻辑继续采用简明中文注释。
