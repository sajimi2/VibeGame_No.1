# 当前状态 · 2026-09-18

用户授权以最新 3D 正交战场为准瘦身和维护性重构，并明确确认旧 2D 只留 Git 历史。

- 已先把完整现状推送 GitHub：`9c4dc82`，标签 `backup/pre-3d-cleanup-20260917`。
- 当前分支 `codex/3d-maintainability`；旧报告、2D 专用代码/资源/测试移除，保留当前 3D 试验场和 15 个有效回归项目。
- 默认主场景及 `play.bat dev` 改为荒堡战场。地图、3 守卫 + 2 弓手、正交相机、移动中攻击、拉弓和任务流程保持。
- `scripts/world/level.gd` 负责装配，地形、HUD、试验场地图独立；战场不再继承试验场地图。
- 背包 UI 与成长分离，结算显式注入依赖；武器数值集中在 `data/weapons/*.tres`；存档 I/O 独立。
- v1 存档路径、JSON 字段、项目身份、库存快照和物品 ID 不变。第三方 Godot AI 与个人配置未改。
- 规模：业务脚本 73 → 34，场景 16 → 2，数据资源 40 → 4，25 份旧报告移除；15 个 3D 专项测试外新增 1 个启动检查。
- 实测：Godot 4.7.2 导入、15 个专项共 279 项断言/路径检查、启动 7 项全通过。核心 174 项；边界 105 项（含 56 条坡道侧缘轨迹，最大单帧高度差 0.12222m）。
- 两个 outpost_sample 旧断言在备份版也失败；修正行走帧数和射线位置后，同一测试在备份版和重构版都 6/6 通过，地图/碰撞/阈值未变。
- 渲染实测：战场启动、试验场登台、背包换装/暂停通过，截图已查看。Windows 战场和试验场均导出成功；战场 PCK 经引擎隔离启动检查 7/7。发布 EXE 不执行外置 --script，因此不把该探针算通过；普通 EXE 曾启动到游戏，自动保存有初始化写入，未交互领奖或换装。
- MCP 按工程路径确认会话；扫描后场景根/掩体均指向新脚本，未新增编辑器错误。移动文件时的旧路径错误仍在历史缓冲中。
- 已有少数测试退出会报 ObjectDB/资源清理警告。用户在重构验收时反馈未发现 bug；后续试玩发现的两处守卫问题见下方修复记录。

- 中文注释已覆盖全部 34 个业务脚本的重要函数，新增 146 处函数说明；自有测试与维护脚本原英文注释也已翻译。注释阶段业务脚本注释约占非空行 8.9%，用户已确认密度合适，后续沿用。
- 注释阶段逐文件比较确认 50 个 GDScript 的可执行文本未变；Godot 导入及隔离启动 7 项通过。中文注释约定已写入 AGENTS.md，并按用户要求保存偏好。
- 验证时修复了 check.ps1 的单元素数组拼接问题（独立提交 f12b0ee），现在无头模式正确传递 --headless 与 --script。修复前误启动过普通游戏，触发原有开局自动保存，未进行交互。

## 守卫刀光与盾挡警觉修复 · 2026-09-18

- 刀光错位：`swing_trail.gd` 将刀刃世界坐标直接写为网格顶点，但 `top_level=true` 仍保留节点入树时的变换，渲染时再次叠加守卫出生偏移。三名守卫实测误差约 12.73m / 2.24m / 12.21m；现显式将刀光全局变换设为单位变换，横斩/突刺及移动转身后误差均低于 0.001m。已查看三名守卫的修复后渲染截图。
- 盾挡不警觉：`receive_strike()` 原先只在未格挡分支刷新受击记忆，站岗守卫因此漏掉远程来袭。现由 `notice_strike()` 统一处理存活受击者的感知；未看见射手时沿来袭方向调查 2.5m，看到后追击。盾挡仍承受三成伤害且无受击硬直，不读取隐藏射手的精确位置。
- 漏测反思：之前把“刀光节点存在/动作与伤害正确”当成了视觉正确，未核对非原点出生时的最终世界顶点；已有盾挡测试从追击状态开始，未覆盖视距外来袭唤醒站岗守卫。后续此类验证须组合实际出生变换、最终画面和被动状态下的事件入口。
- 本轮验证：新增 `guard_reaction` 在修复前 28 项中 17 项失败，修复后渲染与无头模式均 28/28；相关 `attack_motion` 15/15、`combat_polish` 15/15、`guard_encounter` 28/28、导入与隔离启动 7/7 通过。未重跑完整边界套件；未替代用户手感试玩。MCP 当前只列出独立学习工程，本轮运行证据来自本工程独立 Godot 进程，未操作另一工程。
- 用户已于后续试玩确认这一步通过；开始人物与环境美术调整前先提交此修复作为回退点。

## 人物与石材美术调整 · 2026-09-18

- 用户已验收守卫修复，先提交本地回退点 `9b00f06`；美术修改尚未提交，用户随后认可整体效果及巨石/矮墙方向。用户明确选择保留人物、树的像素细度，优先统一石墙、巨石。
- `character_pose.gd` 统一人物关节与握点；头部缩小、腰线提高、腿部加长，保留十二朝向。玩家新增约 0.16 秒、六级姿态的蹲起与蹲行，取消整张纸片缩放；原碰撞高度、站起净空与移动规则不变，敌人无新增下蹲行为。
- 玩家/敌人武器握柄与本帧手心对齐，垂剑到平举平滑衔接；刀光采样实际剑刃。伤害、攻击窗口、射程、箭矢物理起点与物品参数未改。
- 石墙/巨石改用有限色阶像素纹理和分档光照，巨石平滑显示法线；保留几何、碰撞与真实阴影。没有整幅画面降采样；外轮廓仍是三维几何的原生分辨率栅格化，未宣称已完成逐像素手绘剪影。
- 本轮 `character_art` 33、`attack_motion` 15、`guard_reaction` 28、`combat_polish` 15、`height_lab` 20、`space_combat` 27、`tower_feedback` 5、`battlefield` 17，共 160 项通过，导入与隔离启动 7 项通过。已查看十二朝向/蹲起图集、战场及持剑/挥击近景；握柄投影最大偏差约 0.00014px。`combat_polish` 无头退出有 2 个 ObjectDB 资源清理警告，没有脚本错误。本轮 MCP 未列出连接会话，运行证据来自独立 Godot 进程。
- 新增 `docs/ART_PIPELINE.md` 记录资产位置、程序生成形式、Dead Cells / Slice Back / Godot 3D 像素方案的一手资料及本轮取舍；预览图仅输出到忽略目录 `work/`。

## 贴石卡住、疾跑跳跃与深度修正 · 2026-09-18

- 用户报告玩家蹲姿/敌人贴石卡住。原移动直接使用巨石凹凸三角壳，底面与地板重合；实测 240 条进退轨迹有 66 条卡住，接触记录反复返回地板法线。现移动改为填平下沿倒扣并向地下延伸 0.08m 的凸包，保留原外观、位置及精确石面射线。角色/导航共同查询 `1 | 32`，移动代理不参与阻弹/视线。
- 新增 `rock_collision`：240 条站立/蹲姿进退、40 条实际守卫绕石返回、10 次阻弹面检查，290/290 通过；战场任务路线及守卫登台仍通过。
- Shift 疾跑 6.8m/s，步行 4.2m/s、蹲行 2m/s 不变；Ctrl 接替原 Shift 的精确射击。新增十二朝向八相位疾跑，大幅摆腿、抬膝、前倾；按真实位移播帧，顶墙停步，仍支持移动攻击。
- 起跳初速 5.3 → 6.6m/s，实测跳高约 1.05m；十二朝向有蹬地、收腿上升、顶点、下落伸腿、落地缓冲。无连跳/跳跃无敌，不能直接跳上 2m 台，顶头和坡上落地正常。
- `character_billboard.gd` 统一玩家/敌人直立纸片、相机俯角补偿及本帧剪影投影。修复朝镜头倾斜的纸片插进后方墙面的深度问题；武器握点同帧转换。去掉胶囊太阳投影，改用带透明裁剪的当前人物帧，保留脚底小接触影与原有遮挡轮廓。
- 实际渲染对照：恢复旧 billboard 模式可复现墙前身体被切掉；新模式墙前本体 418/418 采样保留，墙后露出部分 217/418。已查看十二朝向跑/跳图集、站蹲阴影及墙前/墙后图。跑跳含空中挥刀的握点误差 < 0.1px。
- 本轮专项：`character_art` 33、`locomotion_art` 35（渲染，含 33 个基础检查）、`rock_collision` 290、`mission` 29、`battlefield` 17、`height_edge` 56、`height_lab` 20、`combat_polish` 15、`guard_reaction` 28、`attack_motion` 15、`feedback_edges` 8、`guard_encounter` 28、`space_combat` 27、`letter_interaction` 5，共 606 项通过；导入与渲染启动 7/7。未重跑其余库存/结算套件；没有改存档。
- 验证边界：加速固定帧的 `guard_encounter` 音频输出检查曾失败，改用正常实时运行后 28/28（含实际音频输出）通过；不能用加速物理时间替代音频设备时间。新动作专项的无头模式关闭其测试实例音频，避免退出时未播完声音的资源警告。已有个别测试仍有音频资源退出清理警告，无脚本错误。
- 反思记录见 `docs/ART_PIPELINE.md`：同形三角面不等于稳定的移动碰撞，屏幕外观正确不等于三维深度正确。后续分别检查贴边退让、真实 AI 路线及最终渲染；中文注释继续沿用已确认密度。
- 本轮 MCP 未列出任何会话，未声称编辑器远程验收；运行证据来自当前工程的隔离 Godot 进程。新增内容待用户试玩，尚未 commit/push。

## 后退跳膝盖与人工图集预览 · 2026-09-18

- 用户报告鼠标朝向与跳跃移动相反时膝盖反弯。源码确认膝盖误用了 `travel_axis`；现只用它决定脚的摆动，膝盖朝身体局部 +Z 弯曲，再统一按瞄准朝向投影。疾跑同类偏移也修正；未改物理/战斗规则。
- 漏测反思：旧图集仅检查同向前进。新增 12×12×(5+8)=1872 个姿态的双膝检查，汇总 24 项在修复前均失败、修复后通过；实际鼠标输入后退跳与空中转向也通过，已查看前后图集及游戏渲染。
- 新增独立 `tools/art_preview.tscn`：Godot 双击打开后 F6，可切动作、身体/相对移动方向和上肢姿态，点击选帧、播放、逐帧查看、缩放并导出。导出到 `work/art_atlases/`，每格原始 32×48，时间戳防覆盖；只调用现有 Art/Pose，不加载关卡或存档。此阶段尚无 PNG 回导；现已由下方图集工作台阶段接通，使用说明见 `docs/ART_PIPELINE.md`。
- 本轮 Godot 导入通过；`locomotion_art` 无头 60/60、渲染 62/62；`character_art` 33/33（加速无头退出有 3 个已有音频资源清理警告，无脚本错误）。预览器在独立渲染实例中验证 6 种图集尺寸、鼠标选帧、播放、实际 PNG 导出与像素一致性，11/11，通过并查看截图；未重跑无关碰撞/任务套件。
- MCP 仍无连接会话，以上来自隔离引擎进程。已有未提交美术修改保留；本轮未 commit/push。

入口和运行方式见 README.md，代码数据流见 docs/ARCHITECTURE.md。

## 通用图集工作台、手绘回导与箭矢附着 · 2026-09-18

- 用户本轮授权四项实现，保留此前已验收且未提交的美术/移动修改；未 commit/push，未改存档/伤害/攻击时序/第三方插件。
- `scripts/art/` 分离来源协议、规范帧键、图集文档与持久化。工作台扫描 `data/art_sources/*.tres`，接入玩家、守卫、弓手和箭矢；新增同规格人形资源配对应 `art_id` 即可扩展，非人形通过来源适配器接入，UI 不写角色类型分支。
- F6 入口仍是 `tools/art_preview.tscn`。导出原尺寸 PNG + JSON → 外部编辑 PNG → 载入 JSON 预览/修正握点 → 应用 → 重新 F5。每份只覆盖所选动作/选项的精确帧，其余程序生成；支持重复覆盖及按资产恢复。生效资源写 `data/art_overrides/`，PNG 字节嵌入 `.res`，不依赖 work 原稿；本轮没有留下测试色标或正式覆盖资源。
- 回导校验包含来源/动作/帧键、尺寸、重复键和锚点；JSON 数字显式恢复来源选项类型，避免 float 与整数列表不匹配。目录重载方法命名 `reload_catalog`，避免与 GDScript 自身 `reload` 冲突。重复回导及独立新进程读取均实测通过。
- 悬空箭根因：命中只停止世界坐标，根节点又不随 `facing` 旋转。`impact_attachment` 记录相对身体/盾变换并晚于角色移动更新；盾面落点与胶囊命中分离，身体箭深入纸片主体，修复仅跟随变换仍在侧面留空隙的问题。角色身上的箭保留至死亡/卸载，环境/飞行箭沿用五秒寿命。
- `arrow_art.gd` 用 32×12 像素侧影绘制金属箭尖、木杆和尾羽；交叉透明裁剪面保留三维俯仰和深度。飞行箭与搭弓箭共用贴图，也可由工作台导出/回导。箭根原点为箭尖，不再绕偏置箭杆中心旋转。
- 本轮实测：`atlas_pipeline` 无头/渲染 52/52，独立新进程回读 2/2；`arrow_attachment` 渲染 33/33（含 29 个基础检查、两组十二朝向实景与墙前/墙后像素比较）。相关 `character_art` 33、`locomotion_art` 60、`attack_motion` 15、`guard_reaction` 28、`space_combat` 27、`combat_polish` 15、`mission` 29，共 207 项通过；导入与隔离启动 7/7。未重跑无关库存/导航全套，未宣称本轮已导出发布包。
- 已查看工作台、盾箭/身体箭十二朝向截图；新增回归加入 core。反思与新资产接入示例记录在 `docs/ART_PIPELINE.md`，验证说明见 `tests/README.md`。本轮 MCP 两次 list 均无会话，运行/渲染证据来自本工程独立 Godot 进程，尚待用户实际手感验收。
