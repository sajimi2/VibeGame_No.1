# 当前状态 · 2026-09-18

用户授权以最新 3D 正交战场为准瘦身和维护性重构，并明确确认旧 2D 只留 Git 历史。

- 已先把完整现状推送 GitHub：`9c4dc82`，标签 `backup/pre-3d-cleanup-20260917`。
- 当前分支 `codex/3d-maintainability`；旧报告、2D 专用代码/资源/测试移除，保留当前 3D 试验场和 15 个有效回归项目。
- 默认主场景及 `play.bat dev` 改为荒堡战场。地图、3 守卫 + 2 弓手、正交相机、移动中攻击、拉弓和任务流程保持。
- `scripts/world/level.gd` 负责装配，地形、HUD、试验场地图独立；战场不再继承试验场地图。
- 背包 UI 与成长分离，结算显式注入依赖；武器数值集中在 `data/weapons/*.tres`；存档 I/O 独立。
- v1 存档路径、JSON 字段、项目身份、库存快照和物品 ID 不变。第三方 Godot AI 与个人配置未改。
- 规模：业务脚本 73 → 34，场景 16 → 2，数据资源 40 → 4，25 份旧报告移除；15 个 3D 专项测试外新增 1 个启动检查。
- 实测：Godot 4.7.2 导入、15 个专项共 279 项断言/路径检查、启动 7 项全通过。核心 174 项；边界 105 项（含 56 条坡道侧缘轨迹，最大单帧高度差 0.12222m）。
- 两个 outpost_sample 旧断言在备份版也失败；修正行走帧数和射线位置后，同一测试在备份版和重构版都 6/6 通过，地图/碰撞/阈值未变。
- 渲染实测：战场启动、试验场登台、背包换装/暂停通过，截图已查看。Windows 战场和试验场均导出成功；战场 PCK 经引擎隔离启动检查 7/7。发布 EXE 不执行外置 --script，因此不把该探针算通过；普通 EXE 曾启动到游戏，自动保存有初始化写入，未交互领奖或换装。
- MCP 按工程路径确认会话；扫描后场景根/掩体均指向新脚本，未新增编辑器错误。移动文件时的旧路径错误仍在历史缓冲中。
- 已有少数测试退出会报 ObjectDB/资源清理警告；不是本轮新增脚本错误。用户本轮已完成玩法试玩，反馈没有发现 bug。

- 中文注释已覆盖全部 34 个业务脚本的重要函数，新增 146 处函数说明；自有测试与维护脚本原英文注释也已翻译。业务脚本注释约占非空行 8.9%，先由用户阅读评估密度。
- 本轮逐文件比较确认 50 个 GDScript 的可执行文本未变；Godot 导入及隔离启动 7 项通过，无需重复全套玩法测试。中文注释约定已写入 AGENTS.md，并按用户要求保存偏好。
- 验证时修复了 check.ps1 的单元素数组拼接问题（独立提交 f12b0ee），现在无头模式正确传递 --headless 与 --script。修复前误启动过普通游戏，触发原有开局自动保存，未进行交互。

入口和运行方式见 README.md，代码数据流见 docs/ARCHITECTURE.md。用户已确认重构后的玩法验证通过；当前等待读码后的注释密度反馈，不自动增加新系统。
