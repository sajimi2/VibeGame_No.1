# 行囊界面画稿

内置 `image_gen` 生成，完整提示词在 `prompts/`，未使用CLI/API回退。

- `ui_panel.png`：暗板岩、旧铁雕纹边框与织物内底；文字/物品由独立控件绘制。背景不拦截鼠标。
- `equipment_anatomy.png`：自然成人比例、正面中性人形，保留真实透明度，用作七部位装备的部位示意。不是实时人物换装预览。

UI设计画布固定1280×720；中文使用清晰系统CJK字体，背景/图标用最近邻显示。世界已取消整屏降采样；交互名称也以2D界面文字显示，不随镜头缩小。
