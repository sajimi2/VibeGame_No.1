# T05 实现报告

- 状态：READY_FOR_REVIEW（其中契约方案待 Codex 裁定，见第 5 节）
- 实现者与日期：DeepSeek Harness，2026-09-14
- 接口是否改变：**是**。`ProgressionPort` 追加 `enum Attribute` 与 `spend_point()`，记录见 `reports/CHANGE_REQUEST.md` 变更 5。
- 流程说明：沿用用户「跳过逐任务审查门」指示，T02～T04 未 ACCEPTED 即推进。

## 1. 交付内容（TASKS.md T05）

| 要求 | 实现 |
|---|---|
| ProgressionPort | `scripts/progression/actor_progression.gd`（`ActorProgression`） |
| 死亡一次发奖 | `LootSpawner` 在遭遇的 `enemy_spawned` 上挂接死亡；每只敌人自带 `experience_reward`（盗匪 20 / 弓箭手 16 / 野兽 18） |
| 升级选择 | `scripts/ui/level_up_panel.gd`：显示等级/经验/未分配点，两个按钮 +5 最大生命 / +5 最大体力 |
| 等级上限 5、需求 50*L | `MAX_LEVEL = 5`、`XP_PER_LEVEL_FACTOR = 50`，`xp_to_next()` 在满级返回 0 |
| 每级 +1 点 | `POINTS_PER_LEVEL = 1`，跨级按顺序逐级发 `leveled_up` |
| 加点上限生效 | `ActorCombatant` 增加 `progression_path`，`max_health()`/`max_stamina()` = 基础 + 装备 + 加点 |

## 2. 执行命令、退出码与日志

| 命令 | 退出码 | 结果 |
|---|---|---|
| `.\scripts\check.ps1` | 0 | 全 PASS |
| `--headless --script res://tests/t05_progression_test.gd` | 0 | checks=46 failures=0 |
| 其余 8 个套件 | 0 | 全部 0 失败 |
| `--path .`（真实窗口 10 秒） | 0 | 无 ERROR/WARNING |

合计无头断言 **414 项 0 失败**。

## 3. 验收结果与证据

| TASKS.md 验收项 | 证据 |
|---|---|
| 一次奖励跨多级不丢经验 | 170 经验 → 等级 3 且**余 20**（50+100 后剩 20），未被丢弃 |
| 每级事件仅一次 | 跨两级恰好发 2 次 `leveled_up`，顺序为 2→3；精确到阈值只发 1 次 |
| 重复死亡不发奖 | 对已死目标再次 `receive_hit` 不再增加经验；`died` 契约本身恰好一次 |
| 满级不死循环 | 10 万经验后等级锁定 5、`xp_to_next()==0`、经验清零；再给 5×500 经验等级与点数都不变 |
| 加点不免费治疗 | 先受伤再升级加点：最大生命上升，当前生命**不回升**；体力同理 |
| 负数/无点请求拒绝无副作用 | 无点时两种加点都返回 false 且快照逐字节不变；未知枚举值返回 false 且不消耗点 |

另：`grant_experience(0)` 与负数被忽略；每级恰好给 1 点；两点分别投生命与体力后加成各为 +5。

## 4. 未验证内容及原因

1. **真人操作升级面板**：按钮流程由逻辑断言覆盖，未真人点击；面板未做视觉调优。
2. **经验曲线手感**：敌人 16～20 经验、需求 50/100/150/200 是否合适，属主观数值（用户已明确延后）。
3. 未做「满级后经验归零」之外的满级表现提示（如 HUD 提示已达上限）。
4. 未在 macOS/Linux 运行。

## 5. 需要 Codex 裁定：契约方案

TASKS.md 要求 `spend_point` 接口与加点枚举由 Codex 先补定。为不停摆整条 T03→T07 链路，我提出了最小方案并实现（`enum Attribute { MAX_HEALTH, MAX_STAMINA }` + `spend_point(attribute) -> bool`），细节与语义写在 `reports/CHANGE_REQUEST.md` 变更 5。若命名或签名需要调整，改动集中在 `scripts/contracts/progression_port.gd` 与 `scripts/progression/actor_progression.gd` 两个文件，我可以立即改名。

## 6. 已知问题

- 升级面板常驻显示（未做「升级时才弹出」）；点数耗尽时按钮置灰。
- 经验只在敌人死亡时给，尚未接入任务/探索奖励（T06 需要时再加）。
- `ActorProgression` 没有存档接口（T07 才需要）。
