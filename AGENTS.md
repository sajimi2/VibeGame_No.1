# OutpostRPG 协作规则

这是个人 Godot 小体量游戏，用户决定方向并试玩。引擎为 Godot 4.7.2、GDScript、Compatibility。当前主线是固定斜角正交的三维战术原型，当前战斗入口为 `scenes/courtyard_combat.tscn`，旧荒堡/驿站/高度场已按2026-09-23用户要求移出活跃目录，仅在 `tests/fixtures/legacy/` 保留回归夹具；旧 2D 四关和早期报告已按用户要求移出活跃工程，完整历史在备份标签中。F5 默认进入小院营地—林路—灰榆驿站探索篇章。

## 开始工作
- 先读 `HANDOFF.md`（接手入口）、`AI_SYNC.md`（阶段状态），检查实际文件及 Git 差异；只按当前请求推进。
- 当前方向看 `docs/TACTICAL_HEIGHT_PLAN.md`。架构看 `docs/ARCHITECTURE.md`；旧 T01～T07 文件在 Git 历史中，不恢复旧派发流程。
- 保留所有已有未提交修改和已确认回退节点。未经本轮要求不重置、清理工作区或自动提交/推送。

## 用户理解与控制权
- 正式地图摆放以 `scenes/courtyard_combat.tscn` 的 `Map` 为准，物件模板在 `scenes/props/`；不得从旧坐标表重新生成并覆盖人工编辑。固定物件应优先做可在编辑器选择/移动/保存的场景。用法见 `docs/EDITING_MAP.md`。
- 用户有 C 和嵌入式基础，正在学习 Godot 的 Scene / Node / Signal / Resource / Autoload 与 GDScript；用当前项目的小例子解释，不把不熟悉 Godot 等同于不懂编程。
- 优先最小可验证修改，不为规范而大规模重构。AI 可以承担重复实现、UI、工具、配置及 Debug；用户应理解 Player、战斗、状态机、核心数据、存档和全局管理的主流程。
- 新增核心模块前先说明职责、输入输出、与现有模块的依赖，再实现。较大修改说明改了什么、为什么、数据流、谁调用谁、影响范围及验证结果。
- 用户无法理解的核心逻辑先梳理，再决定是否加功能。讲解应结合当前代码；按用户当轮授权决定只读或实现范围。
- 常规小修在用户授权范围内自主完成，无需逐行请示；不因工具可用而推断获得批量节点改写、大范围重构或无人监督开发授权。
- 避免继续扩大场景总控职责、跨层直接控制 UI、隐式硬引用和重复系统。发现现有耦合如实说明，按实际修改需要逐步处理，不先搭重型框架。

## 中文注释约定
- 用户要求：今后新增或修改任何重要逻辑，都要配简明扼要的中文注释；已有自有代码的英文说明统一为中文。
- 重要函数通常用 1～2 行说明职责、调用时机或关键输入输出；复杂分支解释原因、状态变化和边界约束，不逐行复述普通赋值。
- 注释必须与实际实现一致，改逻辑时同步更新；保留 API、变量、资源路径等原始标识符，避免翻译导致查找困难。
- 用户已确认当前中等偏低的注释密度合适，后续沿用；第三方插件仍遵守不修改内部代码的约定。

## 产品与验证
- 已确认场景美术基线（2026-09-20）：固定单视角完整绘画资产 + 简单隐藏盒/柱/灰模负责空间。活跃资产在 `assets/environment/painted/`，基准 `scenes/painted_courtyard.tscn`。允许美术/碰撞适度不吻合；不恢复逐面生图贴满模型或对每件资产脚本精雕的退役路线。制作原图、提示词、像素整理和配准必须随资产保留。
- 像素规格以 `docs/PIXEL_ART_STANDARD.md` 为准（2026-09-23用户新要求）：标准画面1280×720，禁止整屏二次降采样；UI仍用1280×720清晰中文。新环境固定28逻辑像素/视平面米，向原粗像素横墙/巨石统一，禁止以高清写实素材为基准。所有生图必须保持像素块面、色盘、光向、接地和尺度统一。小幅风摆走已有入口，不移动碰撞；统一阴影继续沿用。
- 已确认美术基线（2026-09-19）：角色以 Blender 保存模型/骨架/动作，导出 GLB 后由 Godot 离线烘焙 96×96 十二朝向图集；运行时只播放颜色/深度及握点。沿用 `VIEW_ZOOM = 1.2` 的屏幕大小，升分辨率不放大世界模型。以已保存的 `.blend` 为制作源，日常导出不重建或覆盖人工动作；不恢复已移除的直接二维人物生成器。
- 当前地图服务于探索、线索、搜刮取舍与有后果的选择；维持可读战斗，不继续以动作/画面横向打磨代替完整体验。保持少量有特点、可观察并应对的敌人，不走刷怪堆量路线。
- 保持正交、十二朝向和移动中攻击。透视/可旋转镜头路线已搁置。配色和人物比例仍开放，参考素材不照抄。
- 改动后验证相关导入、启动与专项测试，不每次重跑全部套件。历史通过记录不等于本轮已验证；主观手感由用户试玩判断。
- 接口或存档改变必须说明影响并兼容已有数据。自动测试使用隔离进度，避免改写玩家真实存档。
- 阶段交付简短更新 `AI_SYNC.md`。不提交密钥、个人工具配置、引擎、导出包或缓存；不将第三方插件内部代码当作游戏业务代码修改。

## Godot MCP
- 接入说明见 `GODOT_AI_SETUP.md`。工具服务名为 `godot-ai`；聊天在 Codex，Godot 中为工具连接面板。
- 先通过 `session_manage(op="list")` 按完整工程路径匹配会话，再读取编辑器状态。每次操作显式携带该工程的 `session_id`，重开编辑器后重新获取，不依赖“当前激活”会话。
- `D:\vibe coding\test project` 是独立学习项目；`OutpostRPG_AgentLab` 是旧接入副本，均不属于本工程修改范围。
- 区分编辑器场景树与运行时场景树；运行后才创建的 Player/敌人不一定存在于编辑状态的树中。代码阅读、MCP 实读、实际运行和用户试玩是不同证据。
