# OutpostRPG 接手入口

**2026-09-23 后续修正：先读 `docs/PIXEL_ART_STANDARD.md`。** 用户要求向低分辨率统一：标准画面1280×720、取消整屏降采样，UI保持清晰中文。树/竖墙按28逻辑像素/视平面米制作，保留粗横墙/巨石基准；同色盘三树形、接地碎叶/土层与打散重复草地已接入。背包使用生成的纹理边框和自然比例人形。人物96²制作源和物理比例不变。

**2026-09-23 最新：** F5 / `play.bat` 进入全屏「失踪信使」林路版：小院安全营地 → 正门大路或北侧林径 → 灰榆驿站 → 南侧回路。新增两种树、垂直石墙、可进残屋、四帧开合箱、独立管事/药师、真实采样音效、像素UI和装备人形。8×6背包、7部位装备、调查与两种结果沿用；驿站新增3×3药材包，药师20钱回收。E交谈/搜查，I行囊/日志，拖动时R旋转，F11全屏切换。旧进度不重置；已经完成的委托不会再发。地图默认平地模式2，粗格/缓坡不是当前方向。


2026-09-20：用户已确认本轮整体美术实验成功，**后续默认沿用固定正交视角的完整绘画资产 + 隐藏简单空间体积路线**。这不是仍待选型的提案，不要重新回到逐面生成图片贴满精细模型的方式。

## 先读与先运行

1. 读 `AGENTS.md`、`AI_SYNC.md`，检查 Git 分支和工作区；只按新一轮用户请求推进。
2. 读 `docs/ARCHITECTURE.md` 的职责、装配顺序和隐藏约定；场景美术看 `docs/ENVIRONMENT_ART.md`，人物看 `docs/ART_PIPELINE.md`，武器看 `docs/WEAPON_PIXEL_EXPERIMENT.md`。
3. 纯美术基准是 `scenes/painted_courtyard.tscn`；独立试玩用 `play.bat` 或 `play_courtyard.bat`，均进入当前林路故事。
4. 当前玩法：F5 → `scenes/courtyard_combat.tscn`；`woodpath_region.gd` 管地图与美术，`woodpath_story.gd` 管空间交互，`expedition_state.gd` 管箱子/日志/事实/钱币。旧荒堡、驿站、高度场退出活跃目录，只留 `tests/fixtures/legacy/` 回归夹具。新开合箱支持搜刮，井仅为景物。
5. 双屋实例隔离是 `tests/fixtures/painted_cottage_scene.tscn` 回归夹具，不是第二条美术路线。

## 必须沿用的管线

- 场景：简单灰模确定尺寸/门洞/高低 → 固定角度参考 → 生图制作完整单视角物件，房屋按屋顶/墙体/内景分层 → 原稿保留 → 离线统一像素密度 → 画稿负责轮廓，隐藏盒/柱/简模负责碰撞、阻弹和遮挡采样。允许适度美术/碰撞偏差，不为细节再建精模。
- 人物：保存的 Blender 模型/骨架/动作 → GLB → Godot 开发期烘焙 96×96、十二朝向的颜色/深度/握点 → 运行时播放。保留 `VIEW_ZOOM=1.2`，不得降低角色分辨率；日常导出不重建或覆盖人工动作。
- 武器/刀光：读取角色世界握点，保留独立实时 GPU 像素化；命中由战斗射线与动作窗口负责，不由图片边缘负责。
- 阴影：新路线统一 `assets/environment/painted/courtyard/shadow_style.tres`；方向、22% 覆盖率、颜色、柔边集中管理。资产只描述体量，未来小幅风摆走 `set_visual_sway`，不动碰撞；默认未开启风摆。
- 遮挡：少量遮挡逐像素灰显被挡部分；屋顶室内整片淡化后叠全透圆、室外仅圆；普通墙累计遮挡 90% 开启/80% 退出。只处理真正挡住人的构件，承托面保护，花草不遮人。
- 新美术以1280×720清晰画面、素材28px/m为标准；当前故事F2仅提示固定规格，角色尺寸和固定镜头不变。

## 版本与删除边界

- 清理前完整成果 **`ddb3b2c`** 已推送 `origin/codex/skeleton-blender-96`，包含所有逐面贴图实验和本轮已验收小院，作为准确回退点。不要为查旧素材直接重置当前分支。
- 已退役：逐面贴图投影器/Shader 分支、旧小屋贴面实验、仓库贴面样板及其专用纹理/配置/测试。旧内容去 Git 历史查询，不恢复到活跃工程。
- 2026-09-23用户授权清理旧地图：旧三场景及脚本从 scenes/、scripts/world/ 移出；专项仍调用的副本集中 tests/fixtures/legacy/。旧BAT已移出活跃目录。共享几何/材质仍供测试或现有组件使用，不连带删除。不要自动恢复旧入口。
- 活跃绘画资产集中于 `assets/environment/painted/`，含原图、提示词、配准、空间简模和运行图。小院土路已从退役目录迁出并保留来源记录。

## 前阶段起点（粗格路线已于2026-09-23停用）

小院院门外沿土路向右，当前默认进入分层粗格地形：4m 一格、0.5m 高度单位，同高合并，只在高差边界生成断崖；坡道必须显式指定方向。连续缓坡经用户视觉反馈后不再作为默认方向，代码和旧测试保留，根节点 `outskirts_mode=1` 可对照。`terrace_seed=0` 是固定验收布局；非零种子只改变南侧小平台及坡道，主路和坑出口固定，不是每局随机。

V 对照灰模，F3 查看 4m 网格与物件碰撞；院内建筑/任务和纯美术 `painted_courtyard.tscn` 不变。地形入口是 `scripts/world/terrace_layout.gd`（数据/外露边）→ `terrace_ground.gd`（显示/碰撞）；最小绘画套件及原图/提示词见 `assets/environment/painted/terraces/README.md`。专项 `terrace_ground_test.gd` 验证合并、半格、坡道、坑出口、实际追击和高台命中，追加 `-- seed=42` 可测随机平台。当前几何规则可用，美术首版仍需用户试玩验收。

小院战斗已接通，接下来优先用户试玩横劈/过顶下劈、独立弓、格挡和翻滚，不重开视觉选型。左键当前武器攻击，右键按住格挡，Alt 翻滚，1–5 换装（第五格弓）。断剑三次未被格挡的有效命中后，下一次攻击释放一次光刃；空挥不蓄能，单次挥击最多计一次。扩展入口和未实现边界见架构文档。自动测试使用隔离存档；美术回归运行 `scripts/check.ps1 -Suite art -Rendered`，玩法回归用 `-Suite core`。既有通过记录不是下一轮的新验证。
